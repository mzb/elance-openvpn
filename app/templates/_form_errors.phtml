<?php if (isset($errors) && $errors): ?>
  <div class="alert alert-error">
    <strong class="alert-heading">
      <i class="icon-exclamation-sign"></i> Correct the following issues:
    </strong>
    <ul>
    <?php foreach ((array)$errors as $msg): ?>
      <li><?php echo $msg ?></li>
    <?php endforeach ?>
    </ul>
  </div>
<?php endif ?>
